version: "3.9"

services:

  ai-recruiter:

    build: .

    container_name: ai_recruiter

    ports:

      - "8501:8501"

    env_file:

      - .env

    volumes:

      - ./artifacts:/app/artifacts

      - ./data:/app/data

    restart: unless-stopped